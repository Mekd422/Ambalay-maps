import CodeBlock from '../../Components/CodeBlock.tsx'

export default function ForwardGeocoding() {
  const curlExample = `curl -H "x-api-key: YOUR_API_KEY" "https://api.ambalaymaps.com/v1/search?query=tom"`;

const jsonResponse = `{
  "status": 200,
  "message": "GEO_CODING_SEARCH_SUCCESS",
  "data": [
    {
      "lat": "9.5983108",
      "lon": "41.8701823",
      "displayName": "Tom cafe, Djibouti Road, ድሬ ዳዋ... ኢትዮጵያ"
    },
    {
      "lat": "12.0355846",
      "lon": "39.0466717",
      "displayName": "Tom Supermarket, Lalibela-Geshena Road, Lalibela... ኢትዮጵያ"
    }
  ]
}`;

return (
<div className="bg-black text-gray-300 min-h-screen p-8 font-sans">
{/* Header Section */}
<header className="mb-10">
<h1 className="text-white text-5xl font-bold mb-4 tracking-tight !mt-0">
Forward Geocoding (Place Search)
</h1>
<div className="h-[1px] bg-gray-800 w-full mb-6"></div>
<p className="text-gray-400 text-lg leading-relaxed">
<strong className="text-white">Purpose:</strong> Searches for locations or addresses by name or text query. This forward geocoding endpoint returns a list of places that match the query string, each with coordinates and a display name.
</p>
</header>

      {/* Endpoint Details */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-4 !mt-0">Endpoint</h3>
        <p className="font-mono bg-[#1a1a1a] p-3 rounded text-[#8cff2e] mb-4 inline-block">
          GET /search
        </p>
        <p className="text-gray-400">
          <strong className="text-white">Full URL:</strong> <span className="text-[#00d1ff]">https://api.ambalaymaps.com/v1/search</span>
        </p>
      </section>

      {/* Query Parameters Table */}
      <section className="mb-12">
        <h3 className="text-[#4d79ff] text-xl font-bold mb-6">Required Query Parameters</h3>
        <div className="overflow-x-auto rounded-xl border border-gray-800 shadow-2xl mb-6">
          <table className="w-full text-left border-collapse">
            <thead className="bg-[#1a1a1a] text-gray-400 text-sm uppercase tracking-wider">
              <tr>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Parameter</th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Type</th>
                <th className="px-6 py-4 font-semibold border-b border-gray-800">Required</th>
                <th className="px-6 py-4 border-b border-gray-800">Description</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-gray-800">
              <tr>
                <td className="px-6 py-4 font-mono text-[#8cff2e]">query</td>
                <td className="px-6 py-4">string</td>
                <td className="px-6 py-4">Yes</td>
                <td className="px-6 py-4 text-sm text-gray-400">
                  The search query text (e.g., <code className="text-white">query=Tom</code>). Can be an address, place name, or business name. The search is case-insensitive and supports partial matches.
                </td>
              </tr>
            </tbody>
          </table>
        </div>
        <p className="text-gray-400 italic">
          <strong className="text-white">Auth Header:</strong> <code className="text-[#8cff2e]">x-api-key: Your API key</code>.
        </p>
      </section>

      {/* Example Request & Response */}
      <section className="mb-12 grid grid-cols-1 gap-8">
        <div>
          <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Example Request</h3>
          <p className="text-gray-400 mb-4 text-sm">Searches for locations matching the term "tom".</p>
          <CodeBlock code={curlExample} language="bash" />
        </div>
        <div>
          <h3 className="text-[#4d79ff] text-xl font-bold mb-4">Example Response</h3>
          <CodeBlock code={jsonResponse} language="json" />
        </div>
      </section>

      {/* Response Explanation */}
      <section className="mb-12">
        <h3 className="text-white font-bold mb-4 text-lg">Response Field Details</h3>
        <p className="text-gray-400 mb-4">The <code className="text-[#8cff2e]">data</code> field returns an array of matching locations, sorted by relevance. Each entry includes:</p>
        <ul className="space-y-4 text-gray-400 leading-relaxed list-disc ml-6">
          <li><code className="text-[#8cff2e]">lat</code> and <code className="text-[#8cff2e]">lon</code> — The geographic coordinates of the location.</li>
          <li><code className="text-[#8cff2e]">displayName</code> — A formatted address or name of the place, often including street, city, region, and country.</li>
        </ul>
      </section>

      {/* Status Codes */}
      <footer className="mt-20 pt-10 pb-20">
        <h3 className="text-[#4d79ff] font-bold text-xl mb-6">Status Codes & Behavior</h3>
        <div className="space-y-6 text-sm text-gray-400">
          <p>
            <strong className="text-white">200 OK (Results Found):</strong> Message is <code className="text-[#8cff2e]">"GEO_CODING_SEARCH_SUCCESS"</code> and data contains an array of results.
          </p>
          <p>
            <strong className="text-white">200 OK (No Results):</strong> Still returns 200 but the <code className="text-[#8cff2e]">data</code> array will be empty (<code className="text-white">[]</code>). Inform the user "no results found" in this case.
          </p>
          <p>
            <strong className="text-white">401 Unauthorized:</strong> Missing or invalid API key. Response: <code className="text-[#ff4d4d]">"API_KEY_REQUIRED"</code>.
          </p>
          <p>
            <strong className="text-white">400 Bad Request:</strong> Occurs if the query parameter is missing entirely.
          </p>

        </div>
      </footer>
    </div>

);
}
